package planspace.chefModelTypes;

public class ChefEnumTypes {
       public enum t_goalClassificationType {
    	   UNKNOWN, NEUTRAL, INCREASED, HIGH, UNACCEPTABLE
       }
       
       public enum t_proveResult {
    	   UNKNOWN, POSITIVE, NEGATIVE
       }
       
       
       public enum t_factResult {
    	   UNKNOWN, TRUE, FALSE
       }
       
       
       public enum t_measurementTaken{
    	   UNKNOWN, TRUE, FALSE
       }
       
       public enum t_impactResult{
    	   UNKNOWN, TRUE, FALSE
       }
       
       public enum t_impactType {
    	   RISK, OPPORTUNITY
       }
}
